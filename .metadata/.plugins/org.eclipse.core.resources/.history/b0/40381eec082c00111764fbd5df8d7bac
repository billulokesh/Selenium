package Pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;

import Basetest.Base;
import elements.Elements;


public class Login {
	private WebDriver driver;
	public Login(WebDriver driver) {
this.driver=driver;
	}
	
	 public void login()
	   {
		   driver.findElement(By.xpath(Elements.loginemail)).sendKeys(Elements.email);
		   driver.findElement(By.xpath(Elements.loginpassword)).sendKeys("Billu@123");
		   driver.findElement(By.xpath(Elements.loginbtn)).click();
		   
	   }
	 

}
